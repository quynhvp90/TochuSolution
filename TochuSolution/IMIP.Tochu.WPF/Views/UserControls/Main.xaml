<UserControl x:Class="IMIP.Tochu.WPF.Views.UserControls.Main"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:IMIP.Tochu.UI.ViewModels;assembly=IMIP.Tochu.UI"
             xmlns:local="clr-namespace:IMIP.Tochu.WPF.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Main View" FontSize="20"/>

        <TextBox Grid.Row="1"
                 Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,10"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="Search"
                    Command="{Binding GoSearchCommand}"
                    Width="100"/>

            <Button Content="Master"
                    Command="{Binding GoMasterCommand}"
                    Width="100"
                    Margin="10,0,0,0"/>
        </StackPanel>

    </Grid>
</UserControl>
