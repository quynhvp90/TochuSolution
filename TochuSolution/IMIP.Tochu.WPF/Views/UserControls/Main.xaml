<UserControl xmlns:Editors="clr-namespace:Infragistics.Windows.Editors;assembly=Infragistics.WPF.Editors" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:IMIP.Tochu.WPF.Views.UserControls"
             xmlns:ig="http://schemas.infragistics.com/xaml" 
             x:Class="IMIP.Tochu.WPF.Views.UserControls.Main"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FFD8D8D8" BorderBrush="#DCDDE1" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel>
                    <StackPanel Grid.Row="0" Margin="15,15,15,15">
                        <TextBlock Text="Branch" FontSize="13" Foreground="#636E72" Margin="5,0,0,5"/>
                        <Editors:XamTextEditor Name="txtBranchCode" Text="{Binding BranchCode}" Style="{StaticResource BootstrapXamEditor}" NullText="Branch: 310"/>
                    
                    </StackPanel>
                    <Separator Background="#FFC5C5C5" Margin="0,10"/>
                </StackPanel>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Button Style="{StaticResource MenuButtonStyle}" Click="Search_Click">
                            <StackPanel>
                                <TextBlock Text="Search" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource MenuButtonStyle}" Click="Master_Click">
                            <StackPanel>
                                <TextBlock Text="Master" FontSize="16" FontWeight="Bold"  HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </ScrollViewer>

                <Button Grid.Row="2" Content="ĐĂNG XUẤT" Height="40" Background="#F1F2F6" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="#EBF2FA">
            <Frame x:Name="InternalFrame" NavigationUIVisibility="Hidden">
                <Frame.Content>
                    <Grid>
                        <TextBlock Text="Vui lòng chọn chức năng" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#A4B0BE" FontSize="20"/>
                    </Grid>
                </Frame.Content>
            </Frame>
        </Border>
    </Grid>
</UserControl>
