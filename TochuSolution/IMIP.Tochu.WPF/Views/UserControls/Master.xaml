<UserControl x:Class="IMIP.Tochu.WPF.Views.UserControls.Master"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:IMIP.Tochu.WPF.Views.UserControls"
             xmlns:loc="clr-namespace:IMIP.Tochu.WPF.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0" Background="Aqua">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{loc:Loc Key=Master_Title}" FontSize="20"/>

        <TextBox Grid.Row="1"
                 Text="{Binding MasterName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,10"/>

        <Button Grid.Row="2"
                Content="{loc:Loc Key=Btn_Back}"
                Command="{Binding GoBackCommand}"
                VerticalAlignment="Top"/>
    </Grid>
</UserControl>
